# Pipeline Resources

Small helper resources for pipeline scripts can live here.

Large corpus resources, such as `unigram_freq.csv`, should live under:

```text
data/resources/unigram_freq.csv
```

`04_pes2o_filter_clean.py --strict` requires that unigram frequency file.
