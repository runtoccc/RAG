# Local Vector Database

The Chroma vector database is generated locally and is not committed to git.

Build it with:

```bash
python scripts/rebuild_index.py
```

Inspect it with:

```bash
python scripts/inspect_vector_db.py
```
