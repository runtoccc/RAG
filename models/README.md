# Local Models

Local embedding model weights are stored here after download.

Model files are ignored by git because they are large and can be regenerated.

Download or verify the default embedding model with:

```bash
python scripts/download_embedding_model.py
```
